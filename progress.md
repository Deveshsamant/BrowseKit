# BrowseKit — Progress

Status legend: ✅ done · 🟡 partial · ⬜ not started

## Phase 0 — Foundation ✅

Verified by `npm run check` (policy validator + 22 unit tests) and
`npm run smoke` (13 checks in real Chromium with the unpacked extension).

| Area | Status | Notes |
| --- | --- | --- |
| MV3 manifest | ✅ | Only `storage` permission; strict CSP; module service worker; popup; dashboard as options page |
| Icons | ✅ | Generated by `scripts/generate-icons.mjs` (no deps) |
| Service worker | ✅ | Top-level listeners, `onInstalled` lifecycle (settings defaults, DB migrations, meta, welcome tab), message router |
| Messaging | ✅ | `{type,payload}` → `{ok,data\|error}`, sender check, error serialisation; `system/ping`, `system/diagnostics`, `system/openDashboard` |
| IndexedDB | ✅ | Schema v1 (collections, vaultTabs, watchLater, sessions, meta), migration runner, transaction helper, generic repository, cross-context change broadcast |
| Settings | ✅ | `chrome.storage.local`, defaults deep-merge, validation, live `onChanged` |
| Theme | ✅ | System / Light / Dark, applied across dashboard + popup live |
| Dashboard shell | ✅ | Sidebar nav, hash router with view cleanup, all 7 sections routable |
| Home | ✅ | Live record counts, storage usage, privacy summary, first-install welcome |
| Settings page | ✅ | Theme, backup export (JSON download), import (validate-first, merge/replace), erase all, diagnostics via SW |
| Feature sections | 🟡 | TabVault, Watch Later, Media Boost, Sessions, Tools show an honest "Not built yet" page with planned features, permissions and browser limits |
| Popup | 🟡 | Opens dashboard/sections; feature quick actions come with each phase |
| Validator | ✅ | Blocks non-allow-listed permissions, host permissions, weak CSP, network/telemetry APIs, eval, `storage.sync`, `tabGroups`, HTML sinks, remote URLs, missing files/imports, inline scripts/styles |
| Tests | ✅ | node:test — settings, messaging router, backup validation, validator |
| Smoke test | ✅ | SW start, welcome tab, install defaults, IDB CRUD, backup round-trip, all routes, theme sync, popup, CSP blocks network (proved against a local server), no chrome://extensions errors/warnings, no console errors |

### Known gaps in the foundation
- No `_locales` / i18n yet (strings are English, inline).
- No packaging script (zip for the Web Store) yet.
- Smoke test needs Playwright + Chromium available in the environment (not a project dependency).

## Phase 1 — TabVault ⬜ (next)

- [ ] Add `tabs` and `favicon` permissions (+ allow-list already covers them)
- [ ] `src/features/vault/` domain module: create/rename/delete/reorder collections; add/move/rename/delete/reorder tabs; `sortOrder` maintenance
- [ ] Save current tab / current window / all windows (skip or flag unsupported schemes; dedupe option)
- [ ] Open collection (current window / new window), open single tab
- [ ] Search across saved tabs (title + URL, client-side over IndexedDB)
- [ ] Dashboard TabVault view (list, drag reorder, move dialog, inline rename)
- [ ] Popup quick actions: save tab/window to a collection
- [ ] Import/export: BrowseKit JSON (collections only) + plain URL list
- [ ] Favicons via `_favicon` URL helper
- [ ] Unit tests for domain logic; smoke checks for save/open

## Phase 2 — Watch Later ⬜
## Phase 3 — Tab Manager & Sessions ⬜
## Phase 4 — Quick Tools ⬜
## Phase 5 — Media Boost ⬜
## Phase 6 — Polish (a11y, i18n, performance, packaging) ⬜

See `architecture.md` §10 for scope and permissions per phase.
